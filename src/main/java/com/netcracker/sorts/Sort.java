package com.netcracker.sorts;

/** The main class for implementing inheritance of two algorithms for sorting the BubbleSort
 * @author Tiutiunyk
 * @see BubbleSortToMin#sort
 * @see BubbleSortToMax#sort
 * @version 1.0
 */

abstract class Sort {

    abstract void sort(int[] a);

    /**
     * The ascending sort method uses the method
     *
     * @param a - number 1 takes
     * @param b - number 2 accepts and stores
     * @return Returns the first number
     */
    int swap(int a, int b) {
        return a;
    }
}